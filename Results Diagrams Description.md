## Results Diagrams Description

The document mentions evaluating the performance of proposed models using standardized metrics such as precision, recall, and F1-score, and demonstrating significant improvements in detection accuracy and contextual awareness compared to traditional methods. However, specific numerical results or data tables are not provided in the document. Therefore, the following descriptions outline the types of results diagrams that would typically be generated based on such research, and what data would be required to create them.

### Diagram 1: AI Model Performance Comparison (Bar Chart)

**Purpose:** To visually compare the performance of different AI models (LSTM, Transformer, GNN) against traditional methods across key metrics.

**Data Needed:**
*   **Categories:** Traditional Method, LSTM, Transformer, GNN
*   **Metrics (Numerical Values):** Precision, Recall, F1-score, Detection Accuracy (for each category)

**Visual Elements:**
*   Bar chart with groups of bars for each model/method.
*   Each group represents a model/method, and bars within the group represent different metrics (Precision, Recall, F1-score, Accuracy).
*   X-axis: Models/Methods.
*   Y-axis: Percentage (0-100%) for each metric.
*   Clear labels for axes and legend for metrics.

### Diagram 2: Anomaly Detection Rate Over Time (Line Graph)

**Purpose:** To illustrate the effectiveness of the AI-enhanced UEBA framework in detecting anomalies over a simulated time period, potentially showing a reduction in false positives or an increase in true positives.

**Data Needed:**
*   **Time Series Data:** Time points (e.g., hours, days).
*   **Numerical Values:** Number of detected anomalies (total), True Positives, False Positives (for both traditional and AI-enhanced systems).

**Visual Elements:**
*   Line graph with time on the X-axis.
*   Y-axis: Number of anomalies or detection rate.
*   Multiple lines representing different metrics (e.g., Total Detections, True Positives, False Positives) for both traditional and AI-enhanced systems.
*   Clear labels for axes and legend for lines.

### Diagram 3: Impact of Dataset on Model Performance (Grouped Bar Chart)

**Purpose:** To show how different publicly available datasets (CERT Insider Threat Dataset, UNSW-NB15, TON_IoT) influence the performance of the AI models.

**Data Needed:**
*   **Categories:** Datasets (CERT, UNSW-NB15, TON_IoT)
*   **Metrics (Numerical Values):** Precision, Recall, F1-score, Detection Accuracy (for each model on each dataset).

**Visual Elements:**
*   Grouped bar chart.
*   Groups represent datasets.
*   Bars within each group represent the performance of different AI models (LSTM, Transformer, GNN) on that dataset for a specific metric (e.g., F1-score).
*   X-axis: Datasets.
*   Y-axis: Percentage (0-100%) for the chosen metric.
*   Clear labels for axes and legend for models.

